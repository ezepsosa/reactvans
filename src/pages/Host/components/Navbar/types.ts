export type Props = {
  buttons: string[];
  largescreen?: boolean;
};

export type NavLinkProps = {
  textdecoration: string;
};

export type NavbarContainerProps = {
  largescreen: string;
};
